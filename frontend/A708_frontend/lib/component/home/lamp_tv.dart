import 'package:flutter/material.dart';

class LampTv extends StatelessWidget {
  const LampTv({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
